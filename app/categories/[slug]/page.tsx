import { notFound } from "next/navigation";import { categories,products } from "@/lib/data";import { ShopClient } from "@/components/shop-client";
export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const category=categories.find(c=>c.slug===slug);if(!category)notFound();return <main className="pt-[72px]"><section className="container-x pb-12 pt-20"><div className="eyebrow mb-5 text-black/45">Category / 0{categories.indexOf(category)+1}</div><h1 className="display text-[clamp(68px,14vw,195px)]">{category.name}</h1><p className="mt-5 text-sm text-black/55">{category.copy}</p></section><ShopClient initial={products} category={slug}/></main>}
