"use client";import Link from "next/link";import { ProductCard } from "@/components/product-card";import { useShopStore } from "@/store/use-shop-store";
export default function Favorites(){const items=useShopStore(s=>s.favorites);return <main className="container-x min-h-[75vh] pb-24 pt-36"><div className="border-b hairline pb-7"><div className="eyebrow mb-5 text-black/40">Saved / {items.length}</div><h1 className="display text-[clamp(64px,11vw,150px)]">Considered</h1></div>{items.length?<div className="grid grid-cols-2 gap-3 py-10 md:grid-cols-4 md:gap-5">{items.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="py-28 text-center"><p className="display text-5xl">Nothing saved yet.</p><Link className="btn-dark mt-8" href="/shop">Browse collection</Link></div>}</main>}
