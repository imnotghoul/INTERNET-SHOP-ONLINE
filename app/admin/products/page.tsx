"use client";import Image from "next/image";import { useState } from "react";import { MoreHorizontal,Plus,Search,X } from "lucide-react";import { toast } from "sonner";import { AdminShell } from "@/components/admin-shell";import { products,formatPrice } from "@/lib/data";
export default function AdminProducts(){const [modal,setModal]=useState(false),[query,setQuery]=useState("");const list=products.filter(p=>p.name.toLowerCase().includes(query.toLowerCase()));return <AdminShell title="Products" kicker="Catalog / {30} products"><div className="bg-bone"><div className="flex flex-wrap items-center justify-between gap-4 border-b hairline p-5"><label className="flex items-center gap-2"><Search size={15}/><input value={query} onChange={e=>setQuery(e.target.value)} className="bg-transparent text-xs outline-none" placeholder="Search catalog"/></label><button onClick={()=>setModal(true)} className="btn-dark !min-h-10"><Plus size={14}/>New product</button></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead className="eyebrow text-black/35"><tr>{["Product","Category","Price","Stock","Rating","Status",""].map(h=><th className="px-5 py-4" key={h}>{h}</th>)}</tr></thead><tbody>{list.slice(0,14).map(p=><tr key={p.id} className="border-t hairline"><td className="flex items-center gap-3 px-5 py-3"><div className="relative h-14 w-11 bg-fog"><Image src={p.images[0]} alt="" fill className="object-cover"/></div><div><b>{p.name}</b><div className="mt-1 text-[9px] text-black/35">NL-{p.id.padStart(4,"0")}</div></div></td><td className="px-5">{p.category}</td><td className="px-5">{formatPrice(p.price)}</td><td className="px-5">{p.stock}</td><td className="px-5">{p.rating}</td><td className="px-5"><span className={`px-2 py-1 text-[9px] ${p.stock?"bg-green-100":"bg-red-100"}`}>{p.stock?"LIVE":"SOLD OUT"}</span></td><td className="px-5"><button onClick={()=>toast("Product actions ready")}><MoreHorizontal size={17}/></button></td></tr>)}</tbody></table></div></div>{modal&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4"><form onSubmit={e=>{e.preventDefault();setModal(false);toast.success("Product draft created")}} className="relative max-h-[90vh] w-full max-w-2xl overflow-auto bg-bone p-7"><button type="button" onClick={()=>setModal(false)} className="absolute right-5 top-5"><X/></button><div className="eyebrow text-black/35">Catalog editor</div><h2 className="display mt-3 text-5xl">New product</h2><div className="mt-8 grid gap-6 sm:grid-cols-2">{["Product name","Slug","Price","Old price","Category","Stock","Sizes","Colors"].map(x=><label key={x}><span className="eyebrow text-black/35">{x}</span><input required={!x.includes("Old")} className="field"/></label>)}<label className="sm:col-span-2"><span className="eyebrow text-black/35">Description</span><textarea className="field min-h-24"/></label></div><button className="btn-dark mt-8 w-full">Create product</button></form></div>}</AdminShell>}
