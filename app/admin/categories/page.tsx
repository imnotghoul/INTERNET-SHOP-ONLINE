"use client";import Image from "next/image";import { AdminShell } from "@/components/admin-shell";import { categories } from "@/lib/data";import { Edit3,Plus } from "lucide-react";import { toast } from "sonner";
export default function AdminCategories(){return <AdminShell title="Categories" kicker="Catalog / Taxonomy"><div className="mb-5 flex justify-end"><button onClick={()=>toast("Category editor opened")} className="btn-dark !min-h-10"><Plus size={14}/>New category</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{categories.map((c,i)=><article key={c.slug} className="bg-bone p-4"><div className="relative aspect-[16/9] overflow-hidden bg-fog"><Image src={c.image} alt={c.name} fill className="object-cover"/></div><div className="flex items-center justify-between pt-5"><div><div className="eyebrow text-black/35">0{i+1} / 6 products</div><h2 className="display mt-2 text-3xl">{c.name}</h2></div><button onClick={()=>toast("Category editor opened")}><Edit3 size={16}/></button></div></article>)}</div></AdminShell>}
