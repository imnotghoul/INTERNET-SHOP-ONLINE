import { notFound } from "next/navigation";import { getProduct,products } from "@/lib/data";import { ProductDetail } from "@/components/product-detail";import { ProductCard } from "@/components/product-card";
export async function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const product=getProduct(slug);if(!product)notFound();const related=products.filter(p=>p.categorySlug===product.categorySlug&&p.id!==product.id).slice(0,4);return <main className="pt-[72px]"><ProductDetail product={product}/><section className="container-x section-y"><div className="eyebrow mb-5 text-black/40">You may also like</div><h2 className="display mb-10 text-[clamp(48px,7vw,90px)]">Related forms</h2><div className="grid grid-cols-2 gap-3 md:grid-cols-4 md:gap-5">{related.map(p=><ProductCard key={p.id} product={p}/>)}</div></section><section className="container-x pb-24"><div className="grid gap-10 border-t hairline pt-10 md:grid-cols-3"><div><div className="eyebrow">Customer notes</div><div className="mt-6 display text-7xl">4.8</div><div className="mt-3 flex gap-1">★★★★★</div></div>{["The weight and cut are exactly right. Substantial without feeling rigid.","It has become my default layer. Clean finish and the shape holds beautifully."].map((q,i)=><blockquote key={q} className="border-l hairline pl-5 text-sm leading-7"><p>“{q}”</p><footer className="mt-5 eyebrow text-black/40">Verified buyer 0{i+1}</footer></blockquote>)}</div></section></main>}
